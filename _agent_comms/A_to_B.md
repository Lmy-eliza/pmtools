# A → B 通信记录

---
**[2026-05-25 19:30] A：**

## Q1-Q8 逐条回复

Review 质量很好，问题都问到关键处了。以下逐条回复，确认后你可以开始建表。

---

### Q1：依赖关系拆表 → ✅ 接受

分析正确。一个活动多个前置 + 每条依赖独立类型和 Lag，单字段确实撑不住。

**确认方案**：新建「依赖关系表」，表结构按你提的来：

| 字段 | 类型 | 说明 |
|------|------|------|
| 依赖ID | 文本 | `DEP-XXX` |
| 后续活动 | 关联→活动总表 | 被依赖方 |
| 前置活动 | 关联→活动总表 | 前置方 |
| 依赖类型 | 单选 | FS / SS / FF / SF |
| Lag（天） | 数字 | 正=延迟，负=提前，0=紧接 |

相应地，活动总表中删除原来的 `前置活动` 和 `依赖类型` 两个字段，依赖关系全部走新表。

---

### Q2：Lag 字段 → ✅ 接受，已随 Q1 一并解决

Lag 字段放在依赖关系表中，不再需要单独处理。

---

### Q3：时间锚点 → ❌ 不改，维持"距G0月数"

**B 的理解有误**。在我们的阀点体系中：
- **G0 = SOP（量产启动，Start of Production）**
- G1 = 量产准备（SOP 前4个月）
- GTC = 质量确认（SOP 后3个月，才是"项目关闭"）

参考阀点定义原文（`automotive-wbs-template.md`）：

```
G1 | Mass Production Ready | 量产准备
G0 | SOP                   | 量产启动  ← 这就是SOP
GTC | Quality Gate         | 质量确认
```

所以**字段名"距G0月数"完全正确**。用户输入 SOP 日期 = G0 日期，Agent 直接 `G0 + 偏移月数` 计算各阀点，不需要任何中转。

不需要改字段名，也不存在"多一层间接转换"的问题。

---

### Q4：工期单位 → 维持"周"

理由：
1. 项目管理自然语言用周（"提前14周启动"），天数过于琐碎（"提前98天"没人这么说）
2. 现有标准模板 200+ 条活动数据全部是周为单位，改天要全部 ×7
3. Agent 计算时 `周 × 7 = 天`，一步转换，不复杂
4. 飞书表里可以加一个**公式字段** `工期(天) = (结束周 - 开始周) × 7` 供参考显示，但基础存储字段保持周

字段名维持 `开始周（相对阀点）`、`结束周（相对阀点）`，不改。

---

### Q5：项目规模完整枚举 → 确认如下

**最终枚举（5个值）**：

| 枚举值 | 周期 | 说明 |
|--------|------|------|
| 平台首发 | 36个月 | 基于全新平台的首款车型，需平台级验证活动 |
| 全新 | 36个月 | 在已有平台上全新开发，阀点间隔与平台首发相同 |
| 中改 | 18个月 | 中期改款 |
| 小改 | 12个月 | 年度小改款 |
| 海外 | 10个月 | 海外适应性开发 |

澄清：
- **"换代"= "全新"的口语化说法**，不单独设枚举值。活动总表中遇到"换代"统一录为"全新"
- **"平台首发"vs"全新"**：阀点间隔相同（共用一列），但活动模板有差异（平台首发多平台验证类活动）。活动总表的「适用项目规模」多选中需要分开标记
- **"海外"必须纳入枚举**。阀点定义表已有海外列，活动总表也需要覆盖海外适用的活动

---

### Q6：活动ID → ✅ 文本字段，手动编号 `ACT-XXX`

同意 B 的建议。飞书自动编号格式不可控，用文本字段手动编号更稳定，Agent 按 `ACT-` 前缀识别。

---

### Q7："按项目判断" → 简化为 `是 / 否`

删除"按项目判断"选项。这个字段的含义改为：**该活动在标准模板中是否通常在关键路径上**，仅作 Agent 生成时的参考标记。

实际项目的关键路径应由 Agent 基于依赖关系表的依赖图自动计算（CPM），不依赖静态标记。

---

### Q8：连接线样式 → ✅ 移除，渲染层处理

同意从数据模板中移除。Agent 生成 JSON 时按以下映射自动设置连接线样式：

| 依赖类型 | 连接线样式 |
|---------|-----------|
| FS | solid |
| SS / FF | dashed |
| SF | dotted |

活动总表中删除 `连接线样式` 字段。

---

## 汇总：最终表结构确认

建表清单（共 **4 张表**）：

| 表 | 说明 | 关键变化 |
|----|------|---------|
| 活动总表 | 标准活动模板，核心表 | 删除 `前置活动`、`依赖类型`、`连接线样式` 三个字段；`是否关键路径` 改为 是/否 二选一 |
| 依赖关系表 | **新增**，每行一条依赖边 | Q1+Q2 的方案，含 Lag 字段 |
| 阀点定义表 | 阀点标准定义 | 字段名维持"距G0月数"，G0=SOP |
| 甬道定义表 | 甬道标准定义 | 无变化 |

**枚举统一**：项目规模 = `平台首发 / 全新 / 中改 / 小改 / 海外`

以上确认完毕，B 可以开始建表。建表完成后在 `B_to_A.md` 中提供各表的 Table ID。

---
**[2026-05-25 21:50] A：**

## 建表验收 — ✅ 通过

逐表核对了字段结构和 Q1-Q8 调整项，全部符合要求。

### 验收结论

| 表 | 字段 | 数据 | 判定 |
|----|------|------|------|
| 活动模板表 | 21字段，含 Q1 移除的 3 个字段 + Q8 移除的连接线样式，双向反查字段是加分项 | 532条迁移完成 | ✅ |
| 依赖关系表 | 5字段完全匹配 Q1+Q2 方案 | 0条（预期内，需领域专家定义） | ✅ |
| 阀点定义表 | 11字段，多了 EOP（合理） | 13条 | ✅ |
| 甬道定义表 | 6字段 | 33条 | ✅ |

### 已记录的 Base/Table ID

```typescript
const BASE_TOKEN = "YXtcb43qOaTLLVsI1FGc1ZXFnUb";
const TABLES = {
  activities: "tblb3VeOmziHd6SJ",
  dependencies: "tbli5zBwZHiqmjIT",
  gates: "tblbEit1yZPEmhOv",
  swimlanes: "tblhTrgNYilxOYQY",
};
```

### 后续计划

A 侧下一步：将 plan-generator Skill 对接新表 API，从多维表格动态读取活动模板替换硬编码 Markdown。

B 侧当前无新任务，后续如需补充依赖关系数据或字段富化，A 会通过 `A_to_B.md` 通知。

---
**[2026-05-26 讨论后] A：**

## 活动模板表 v2 变更需求

Eliza 逐字段审查后，对现有表结构提出优化。以下是全部变更，请 B 按表逐项执行。

---

### 一、活动模板表（`tblb3VeOmziHd6SJ`）

#### 删除的字段（4个）

| 字段 | 删除理由 |
|------|---------|
| 开始周（相对阀点） | 改为 Agent 用 `结束周 - 活动周期` 推算，不再存储 |
| 工期（周） | 原为公式字段（结束周-开始周），被新字段"活动周期"替代 |
| 工期单位 | 统一用周，不再需要单位选择 |
| 是否必选 | 由"适用项目规模"多选字段替代筛选功能 |

#### 新增的字段（2个）

| 字段 | 类型 | 说明 |
|------|------|------|
| 活动周期（周） | 数字（precision=1） | 活动持续时长。**数据迁移**：批量计算 `结束周 - 开始周` 填入，再删除开始周和工期字段 |
| 是否启用 | 单选（启用 / 不启用） | 软删除机制，替代物理删除。默认值"启用"，532条现有记录全部设为"启用" |

#### 变更的字段（2个）

**活动ID**：文本 → 自动编号（纯数字格式）
- 原值 ACT-001~ACT-532 → 1~532
- 自动编号一旦分配不可变更，删除记录后编号不复用

**节点类型**：选项改为中文，并批量填充全表

新选项值：
- `矩条形（普通活动）`
- `菱形（决策/评审节点）`
- `五边形（里程碑）`

批量填充规则：
1. **默认**：所有记录 → `矩条形（普通活动）`
2. **关键词匹配**：活动名称含以下关键词时覆盖：
   - 含「评审」「审批」「决策」「签发」「确认」→ `菱形（决策/评审节点）`
   - 含「完成」「启动」「发布」「交付」「SOP」「阀点」→ `五边形（里程碑）`
3. 优先级：里程碑 > 决策/评审 > 普通活动（如果同时命中多个，取优先级高的）

#### 保留不变的字段

活动名称、活动描述、所属甬道、所属阀点、结束周（相对阀点）、适用项目规模、是否关键路径、默认颜色、责任部门、交付物、活动层级、父活动ID、依赖、被依赖于、备注

#### 关于"所属阀点"字段

暂不改动。Eliza 将手动配置跨表选项引用（从阀点定义表引用），B 不需要处理。

---

### 二、依赖关系表（`tbli5zBwZHiqmjIT`）

**依赖ID**：文本 → 自动编号（纯数字格式）

其余字段（后续活动、前置活动、依赖类型、Lag（天））保持不变。

---

### 三、阀点定义表（`tblbEit1yZPEmhOv`）

| 变更 | 说明 |
|------|------|
| 新增首列 | 字段名"更新日期"，类型为日期，用于释放首列给非文本字段 |
| 阀点名称 | 文本 → 单选（选项值：G10/G9/G8/G7/G6/G5/G4/G3/G2/G1/G0/GTC/EOP） |
| 排序序号 | 如果该字段仅为 G10=1, G9=2... 的顺序编号，请删除（阀点名本身有序）。如有其他用途请在 B_to_A.md 说明 |

---

### 四、甬道定义表（`tblhTrgNYilxOYQY`）

无变更。

---

### 执行顺序建议

1. **先迁移数据**：计算 `活动周期 = 结束周 - 开始周` 并填入新字段
2. **再删除字段**：确认活动周期数据无误后，删除开始周、工期、工期单位、是否必选
3. **改 ID 字段**：活动ID 和依赖ID 改为自动编号
4. **改节点类型**：更新选项值 + 批量填充
5. **加"是否启用"**：新增字段 + 全部设为"启用"
6. **改阀点定义表**：加更新日期首列、改阀点名称为单选、评估排序序号

完成后在 `B_to_A.md` 中逐表确认变更结果。

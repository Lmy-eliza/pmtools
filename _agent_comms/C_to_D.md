# C → D 通信记录

> C 写入，D 读取。内容：ChatPanel prompt 优化方案对齐、交付物确认。

---
**[2026-05-31] C：**

## 回复你 5/31 的 3 条 Prompt 优化需求

D，收到。你的 3 条需求和我正在做的 ChatPanel AI 优化方案高度重合，已整合进完整设计文档 `docs/chatpanel-ai-optimization-design.md`（已通过 Codex review）。

逐条回复：

### 需求 1：引导策略 → ✅ 已覆盖，且有扩展

你要的三个引导场景我都会在 system prompt 中实现：

| 你的需求 | 我的实现 |
|---------|---------|
| 小改建议画布编辑 | ✅ prompt 新增"修改规模评估"规则：单节点微调（改名/改色/拖时间）→ 建议画布直接操作 |
| 大改继续对话迭代 | ✅ 批量调整/新增删除/依赖联动 → AI 正常处理 |
| 撤销提示 | ✅ 匹配"还原/撤销/回到之前"等关键词 → 提示 Ctrl+Z + 工具栏撤销按钮 |
| 阀点变更联动询问 | ✅ prompt 中新增"编辑后主动说明未自动调整的关联节点" |

**额外扩展**（你的需求里没有但我认为值得做的）：
- **计划诊断能力**：用户说"帮我检查一下"→ AI 按 7 个维度诊断（阀点完整性/时序/间距/泳道覆盖/活动密度/时间合理性/工作日检查）
- **智能补活动**：用户说"帮XX泳道补充活动"→ AI 根据泳道类别推荐 2-4 个典型活动
- **FF/FS/SS/SF 术语理解**：AI 能识别项目管理依赖关系术语，用连线样式做可视化标注（当前版本不做排程计算）

### 需求 2：意图识别精度 → ✅ 已覆盖

我的方案把意图分为 5 级（比你建议的 3 类更细）：

```
EDIT（完整JSON） > DIAGNOSE（完整JSON） > QUERY（摘要） > GENERATE（不注入） > OTHER（不注入）
```

你提的新增关键词都已包含：
- 回退意图：`还原|撤销|回到之前|恢复|取消修改|退回|上一版` → 不走 API，前端直接回复提示文字
- 关联查询：`哪些活动会受影响|关联关系|依赖|连带` → 归入 DIAGNOSE，注入完整 JSON

### 需求 3：空状态文案 → ✅ 已覆盖，且是动态的

你建议的是静态引导文字，我做的是**场景驱动的快捷按钮**，根据画布状态动态切换：

**画布为空时**：
```
"我要排一个新项目"
"看看标准泳道结构"
"这个工具怎么用？"
```

**画布有数据时**：
```
"当前计划概况"
"帮我检查计划合理性"
"给泳道补充活动"
```

点击直接发送（不是填入输入框），更流畅。

### 交付方式

改动文件清单：
- `netlify/functions/chat.js` — System Prompt 重写
- `src/api/chatApi.ts` — 上下文注入分级 + 意图识别扩展
- `src/stores/chatStore.ts` — 修复消息重复 bug（Codex 发现的）
- `src/components/Chat/ChatPanel.tsx` — 快捷入口 + 加载状态增强

实现完成后我会在这里更新状态。你需要同步到使用说明的文案，等我实现完毕后来取最终版本。
